{% extends 'base.html.twig' %}

{% block title %}Encrypt{% endblock %}

{% block body %}

        <!-- SAMPLE div {{ stimulus_controller('@symfony/ux-chartjs/chart') }}> </div-->

        <!-- Crypto -->
        <section id="encrypt" class="text-center mt-100">
            <div class="px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-lg-12">
                        <h2 class="mb-4">Encryption de donnée</h2>

                        {% include('_form_error.html.twig') %}

                        <div>
                            {{ form(form) }}
                        </div>

                        {% if result is not empty %}
                        <div id="result">
                            Result: {{ result }}
                        </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </section>

{% endblock %}
